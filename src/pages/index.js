import React from 'react';
import styled from 'styled-components';
import tw from 'tailwind.macro';

const Main = styled.div`
  ${tw`p-8 text-pink-500 rounded-lg shadow-2xl top-0 `}
`;

export default () => (
  <>
    <Main>
      some other text <h1>sda</h1>
      <button>ds</button>
    </Main>
  </>
);
