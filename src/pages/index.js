import React from 'react';

import { Hero, Layout, TestimonialSlider, AboutMe } from '../components/index';

export default () => (
  <Layout>
    <Hero />
    <AboutMe />
    <TestimonialSlider />
  </Layout>
);
