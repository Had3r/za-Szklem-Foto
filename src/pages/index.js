import React from 'react';

import { Hero, Layout, TestimonialSlider, AboutUs } from '../components/index';

export default () => (
  <Layout>
    <Hero />
    <AboutUs />
    <TestimonialSlider />
  </Layout>
);
